{
  "name": "sms-slave",
  "version": "1.0.0",
  "description": "Slave that dequeue data from SQS to send it by sms and save in MySQL database.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Miguel Cueva",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.874.0",
    "dotenv": "^8.2.0",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2"
  }
}